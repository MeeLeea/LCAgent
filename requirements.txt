# ============================================
# LangChainAgent 依赖列表
# ============================================

# ---------- LangChain 核心框架 ----------
langchain>=1.0.0,<2.0.0
langchain-core>=1.0.0,<2.0.0
langchain-openai>=1.0.0,<2.0.0

# ---------- LangGraph (Agent 调度 + Checkpoint) ----------
langgraph>=1.0.0,<2.0.0
langgraph-prebuilt>=1.0.0,<2.0.0
langgraph-checkpoint>=4.0.0,<5.0.0
langgraph-checkpoint-sqlite>=3.0.0,<4.0.0

# ---------- MCP (Model Context Protocol) ----------
langchain-mcp-adapters>=0.3.0,<1.0.0
mcp>=1.9.0,<2.0.0
fastmcp>=2.0.0,<3.0.0
pywin32>=312; sys_platform == "win32"  # mcp 在 Windows 上的运行时依赖（pywintypes）

# ---------- LLM SDK ----------
openai>=1.0.0,<3.0.0

# ---------- 联网搜索 (Tavily) ----------
tavily-python>=0.5.0,<1.0.0

# ---------- 瞬时错误自动重试 ----------
tenacity>=9.0,<10.0

# ---------- 飞书远程控制 ----------
lark-oapi>=1.7,<2.0
pyautogui>=0.9,<1.0
pillow>=12.0,<13.0

# ---------- 定时任务调度 ----------
APScheduler>=3.10,<4.0

# ---------- Web API (供 TS 前端调用) ----------
fastapi>=0.110,<1.0.0
uvicorn[standard]>=0.29,<1.0.0

# ---------- 开发/测试依赖 ----------
pytest>=7.0,<9.0
