[package]
name = "langchainagent"
version = "1.0.0"
description = "LangChain Agent 桌面客户端"
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "langchainagent_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
